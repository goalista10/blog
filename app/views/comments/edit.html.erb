<h1>Edit Comment</h1>
<%= form_for @to_be_updated ,url: comment_path(params[:id],params[:cid]) do |form| %>
  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>